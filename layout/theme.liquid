<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  <style>
    body {
      margin:0;
      font-family: Arial;
      background:#0d1b2a;
      color:white;
      text-align:center;
    }
    .btn {
      background:#00ff88;
      padding:15px 30px;
      border:none;
      font-size:18px;
      cursor:pointer;
    }
  </style>
</head>
<body>
  {{ content_for_layout }}
</body>
</html>
